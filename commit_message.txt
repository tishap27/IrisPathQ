Add: C-Python integration test for matrix manipulation

- C program writes a 3x3 matrix to file(matrix.txt)
- Python script compiles, runs, and reads C output
- Tested transpose operation on imported matrix
- Verify NumPy can process C-generated data