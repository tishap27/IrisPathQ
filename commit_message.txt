Add: waypoint exclusion mechanism for alternative route generation

- Implement astar_with_exclusions() to force routes avoiding blocked waypoints
- Enhance generate_alternative_routes() to create diverse paths via intermediate waypoints
- Integrate calculate_wind_penalty() for fuel-efficient routing
- Add astar_find_route_with_weather() combining geographic distance with weather costs
- Routes now respect weather constraints while generating conflict-aware alternatives


This now enables A* to generate multiple distinct routes per flight that avoid
both weather hazards and route conflicts, preparing the problem for quantum
optimization via QAOA.